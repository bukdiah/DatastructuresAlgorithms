import java.io.*;

class DataItemStrings
{
  private String iData; // data item (key) We could have more data

  public DataItemStrings(String ii) {
    iData = ii;
  }

  public String getKey() {
    return iData;
  }
}